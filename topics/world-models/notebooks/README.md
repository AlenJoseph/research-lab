# Notebooks

Python experiments for intuition — not production training.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install numpy matplotlib jupyter
jupyter notebook notebooks/
```

## `representation_predictor_toy.ipynb` (placeholder)

Minimal 2D latent prediction: context points → predict masked target embedding with MSE. Mirrors I-JEPA logic without vision.

Run after creating the notebook or use the script stub below.

## Script stub

If you prefer scripts over notebooks, adapt `notebooks/toy_predictor.py` when added.
